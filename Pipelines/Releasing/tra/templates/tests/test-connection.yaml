apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tra.fullname" . }}-test-connection"
  labels:
    {{- include "tra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
