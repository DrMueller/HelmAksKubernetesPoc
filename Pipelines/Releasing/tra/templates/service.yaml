apiVersion: v1
kind: Service
metadata:
  name: {{ include "tra.fullname" . }}
  labels:
    {{- include "tra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tra.selectorLabels" . | nindent 4 }}
